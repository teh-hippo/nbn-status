# azure-functions is pinned <2 because version 2.x requires Python >=3.13 and
# Azure Functions Linux Consumption is in feature freeze (no 3.13/3.14 worker
# image). See AGENTS.md "Runtime constraints" for context.
azure-functions>=1.24,<2
azure-storage-blob>=12.24.0
niquests>=3.18.2
